class ProcessorConfigs {
  const ProcessorConfigs(); // Empty for now

  factory ProcessorConfigs.fromJson(Map<String, dynamic> json) {
    // Nothing to parse yet, json is ignored for now
    return const ProcessorConfigs();
  }
}
