// Placeholder class for ImportStateHistory
// In a real scenario, this would have properties and a fromJson if it's complex.
class ImportStateHistory {
  const ImportStateHistory();

  factory ImportStateHistory.fromJson(Map<String, dynamic> json) {
    // Currently no properties to parse for this placeholder
    return const ImportStateHistory();
  }
}
