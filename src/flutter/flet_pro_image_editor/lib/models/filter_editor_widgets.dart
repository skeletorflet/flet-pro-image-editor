import '../utils/json_utils.dart' as utils;

class FilterEditorWidgets {
  const FilterEditorWidgets();

  factory FilterEditorWidgets.fromJson(Map<String, dynamic> json) {
    return const FilterEditorWidgets();
  }
}
