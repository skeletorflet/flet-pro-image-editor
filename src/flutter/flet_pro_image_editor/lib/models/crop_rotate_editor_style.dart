class CropRotateEditorStyle {
  const CropRotateEditorStyle();

  factory CropRotateEditorStyle.fromJson(Map<String, dynamic> json) {
    // If it has properties, parse them. Otherwise, just return const instance.
    return const CropRotateEditorStyle();
  }
}
